# Dépendances pour l'API FastAPI
fastapi
uvicorn[standard]
python-multipart
pydantic 
mcp-server-email
# asyncio
pgvector 
psycopg2-binary 
sqlalchemy


agno  # Votre framework d'agent
sqlite3  # Base de données
pandas  # Manipulation de données
numpy  # Calculs numériques
ollama # Pour les modèles de langage
duckdb  # Pour les requêtes SQL
google-cloud-aiplatform  # Pour l'API Gemini
google-auth  # Authentification Google


streamlit  # Pour app.py (interface Streamlit)
python-jose[cryptography]  # Pour JWT tokens
passlib[bcrypt]  # Pour le hachage des mots de passe
python-dotenv  # Pour charger les variables d'environnement (.env)
openpyxl # Pour traiter les fichiers Excel (.xlsx)
xlrd  # Pour lire les anciens fichiers Excel (.xls)
PyPDF2  # Pour traiter les fichiers PDF
python-docx  # Pour traiter les fichiers Word
lancedb  # Pour la base de données vectorielle
sentence-transformers # Pour les embeddings
requests  # Pour les requêtes HTTP
aiofiles # Pour la gestion asynchrone des fichiers
email-validator  # Pour valider les emails
jinja2  # Pour les templates HTML
python-multipart  # Pour le traitement des formulaires
httpx  # Client HTTP asynchrone
bcrypt # Pour le hachage sécurisé
cryptography # Pour la cryptographie
pyjwt # Pour les tokens JWT


FlagEmbedding # Pour le reranker BAAI/bge-reranker-v2-m3 utilisé dans agent.py
random  # Module intégré Python (pas besoin d'installer)
# inspect  # Module intégré Python (pas besoin d'installer)
# time  # Module intégré Python (pas besoin d'installer)



typing-extensions  # Pour les annotations de type avancées
python-dateutil  # Pour le parsing de dates
six  # Souvent requis par d'autres packages
setuptools  # Pour certains packages
wheel # Pour l'installation de packages